# Production URL (must use https:// and a real hostname, not an IP).
# Leave unset for local development — the app will use ATProto loopback OAuth automatically.
# PUBLIC_URL=https://pronouns.blue

# Optional: private_key_jwt client for production/public deployments.
# Generate with: pnpm gen-key
# PRIVATE_KEY={"kty":"EC","crv":"P-256","x":"...","y":"...","d":"...","alg":"ES256"}

# Optional overrides
# PUBLIC_APPVIEW_URL=https://public.api.bsky.app

