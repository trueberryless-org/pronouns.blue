---
export const prerender = true;
import Layout from '../layouts/Layout.astro';
import { SettingsClient } from '@/components/SettingsClient';
---

<Layout title="Settings – pronouns.blue">
  <SettingsClient client:only="react" />
</Layout>
